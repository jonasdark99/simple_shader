name: simple_shader
description: Using a shader, simply.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'  # Lower minimum requirement

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.0

flutter:
  uses-material-design: true
  shaders:
    - shaders/simple.frag
